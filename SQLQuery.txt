CREATE DATABASE harsDb
GO
USE harsDb
GO
CREATE TABLE tblUser(
	Id INT PRIMARY KEY IDENTITY (1,1),
	UserName VARCHAR(100) NOT NULL,
	Password VARCHAR(100) NOT NULL,
	UserType VARCHAR(10) NOT NULL
);
GO
CREATE TABLE tblCustomer(
	Id INT PRIMARY KEY IDENTITY (1,1),
	CustomerName VARCHAR(150) NOT NULL,
	EmailId VARCHAR(150) NULL,
	PhoneNo VARCHAR(150) NULL,
	Address VARCHAR(150) NULL
);
GO
CREATE TABLE tblItem(
	Id INT PRIMARY KEY IDENTITY (1,1),
	ItemTypeName VARCHAR(150) NOT NULL,
	ItemName VARCHAR(150) NOT NULL,
	Description VARCHAR(500) NOT NULL,
	Brand VARCHAR(150) NOT NULL,
	Model VARCHAR(150) NOT NULL,
	Dimensions VARCHAR(150) NOT NULL,
	Color VARCHAR(150) NOT NULL,
	EnergyConsumption VARCHAR(150) NOT NULL,
	MonthlyFee DECIMAL NOT NULL
);
GO
INSERT INTO tblUser VALUES ('admin', '123','Admin');
INSERT INTO tblUser VALUES ('tanvir', '123','Customer');

SELECT *
FROM tblUser

SELECT *
FROM tblItem